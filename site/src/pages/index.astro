---
import CommandsTable from '../components/CommandsTable.astro';
import FlavorToggle from '../components/FlavorToggle.astro';
import '../styles/global.css';

const title = 'metalmind — unified CLI for Obsidian vault, code graph, and cross-repo intelligence';
const description =
  'Token-efficient, privacy-first Claude Code companion. Obsidian vault capture + local semantic recall, Serena LSP navigation, graphify code graphs, and a cross-repo forge — under one themed CLI.';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#07090c" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content="https://metalmind.mzyx.dev" />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <header class="nav">
      <div class="nav-inner">
        <div class="nav-brand">
          <span class="nav-brand-accent">∴</span>&nbsp;metalmind
        </div>
        <nav class="nav-links" aria-label="Primary">
          <a href="#commands">commands</a>
          <a href="#install">install</a>
          <a href="#how">how</a>
          <a href="https://github.com/Nikitzu/metalmind">github</a>
        </nav>
      </div>
    </header>

    <main>
      <section class="hero container" aria-labelledby="hero-title">
        <div class="hero-eyebrow">
          <span class="scadrial-only">Scadrial · Keeper</span>
          <span class="classic-only">Knowledge · CLI</span>
        </div>
        <h1 id="hero-title">metalmind</h1>
        <p class="hero-tagline">
          Not a wrapper — a connecting tissue. Cross-repo caller↔handler edges. A recall ladder
          that preserves wikilink structure instead of flattening it. A typed installer that
          replaces 14 KB of bash. One mental model over every backend — with a neutral alias
          for every themed verb.
        </p>

        <div id="install">
          <div class="install-block" aria-label="Future install command">
            <span class="prompt">$</span>
            <span>npm install -g metalmind</span>
          </div>
          <div class="install-note">
            <span class="install-chip">Coming to npm</span>
            <span>Clone the repo to try early builds.</span>
          </div>
        </div>

        <div class="hero-actions">
          <a class="btn" href="https://github.com/Nikitzu/metalmind">view on GitHub →</a>
        </div>
      </section>

      <hr class="metal-rule" />

      <section class="container" aria-labelledby="beyond-title">
        <h2 id="beyond-title">Beyond the glue</h2>
        <p>
          metalmind leans on strong open-source engines, but the tissue that connects them is
          what makes it a tool rather than a README. Four concrete things we built on top.
        </p>
        <div class="features">
          <div class="feature">
            <div class="feature-title">
              <span class="scadrial-only">Cross-repo forge with route-match</span>
              <span class="classic-only">Cross-repo groups with route-match</span>
            </div>
            <div class="feature-body">
              Name-match edges union the same symbol across repos. A framework-aware HTTP-route
              matcher (Express / Fastify / Koa / FastAPI / Flask + fetch / axios / got / httpx /
              requests) links caller→handler <em>across services</em>. Every inferred edge carries
              provenance (<code>INFERRED_NAME</code> / <code>INFERRED_ROUTE</code>) so Claude can
              trust-grade what it reads.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">Recall ladder, not a search bar</div>
            <div class="feature-body">
              <code><span class="scadrial-only">tap copper</span><span class="classic-only">recall</span></code>
              escalates deliberately: semantic-only (fast), then
              <code>--deep</code> follows backlinks from the top hit, then <code>--expand</code>
              returns hits with linked context in one call. Recovers the
              <code>[[wikilink]]</code> graph that raw embeddings flatten away, and lets you pay
              for depth only when you need it.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">Typed, reversible installer</div>
            <div class="feature-body">
              Replaces 14&nbsp;KB of overnight bash with a typed pipeline: prereq detection with
              per-failure remediation, atomic JSON writes that preserve your existing MCP servers,
              and a full <code>uninstall</code> that restores your prior output-style, unloads
              launchd, and never touches your notes.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">One mental model, swappable backends</div>
            <div class="feature-body">
              <span class="scadrial-only">Every metal verb routes through a single dispatcher — bronze ↔ graphify,
                steel ↔ Serena rename, copper ↔ vault-rag. Swap a backend later by changing one
                row in the map; call sites don't move. Every themed command has a neutral classic
                alias that always works.</span>
              <span class="classic-only">Every command routes through a single dispatcher — graph ↔ graphify,
                rename ↔ Serena, recall ↔ vault-rag. Swap a backend later by changing one row in
                the map; call sites don't move. Every classic command has a themed Mistborn alias
                for those who want it.</span>
            </div>
          </div>
        </div>
      </section>

      <hr class="metal-rule" />

      <section class="container" aria-labelledby="what-title">
        <h2 id="what-title">At a glance</h2>
        <div class="features">
          <div class="feature">
            <div class="feature-title">
              <span class="scadrial-only">store copper</span>
              <span class="classic-only">save</span>
            </div>
            <div class="feature-body">
              Deposit decisions into your vault — frontmatter, timestamped slug, Inbox by default.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">
              <span class="scadrial-only">tap copper</span>
              <span class="classic-only">recall</span>
            </div>
            <div class="feature-body">
              Semantic search with opt-in <code>--deep</code> backlink traversal and
              <code>--expand</code> hybrid retrieval.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">
              <span class="scadrial-only">burn bronze</span>
              <span class="classic-only">graph</span>
            </div>
            <div class="feature-body">
              Per-repo code-graph queries. Auto-indexes on first use; cached across sessions.
            </div>
          </div>
          <div class="feature">
            <div class="feature-title">
              <span class="scadrial-only">forge</span>
              <span class="classic-only">group</span>
            </div>
            <div class="feature-body">
              Group repos together — the merged graph lights up cross-service call paths.
            </div>
          </div>
        </div>
      </section>

      <hr class="metal-rule" />

      <section class="container-wide" aria-labelledby="commands-title" id="commands">
        <h2 id="commands-title">Commands</h2>
        <FlavorToggle />
        <CommandsTable />
        <p style="margin-top:1rem;color:var(--fg-muted);font-size:0.85rem;">
          Both spellings always work at the CLI. The toggle only changes what you see here.
        </p>
      </section>

      <hr class="metal-rule" />

      <section class="container" aria-labelledby="demo-title">
        <h2 id="demo-title">Install flow</h2>
        <p>
          A single <code>metalmind init</code> drives the whole install — prereq checks, vault
          setup, Docker stack start, MCP registration, launchd watcher, output-style migration.
          Reversible via <code>metalmind uninstall</code>.
        </p>
        <div class="cast-wrap" aria-label="Install flow demo (asciinema cast)">
          <div id="cast-placeholder" class="cast-placeholder">
            <span class="chip">Recording soon</span>
            <span>Install-flow demo coming. Clone the repo and run <code>metalmind init</code> to try it now.</span>
          </div>
          <div id="cast-root" style="width: 100%; height: 100%; display: none"></div>
        </div>
      </section>

      <hr class="metal-rule" />

      <section class="container" aria-labelledby="how-title" id="how">
        <h2 id="how-title">How it works</h2>
        <p>
          Three engines do the heavy lifting; a fourth layer is metalmind's own. Each sits
          behind a typed interface bound to a metal verb — so if an engine gets replaced later,
          your muscle memory doesn't.
        </p>
        <div class="diagram">
          <div class="diagram-node">
            <div class="diagram-node-title">Vault</div>
            <div class="diagram-node-body">
              Obsidian + Qdrant + Ollama (nomic-embed-text). Auto-reindex via launchd watcher.
            </div>
          </div>
          <div class="diagram-node">
            <div class="diagram-node-title">Serena</div>
            <div class="diagram-node-body">
              LSP-backed symbol navigation and coordinated renames. Zero-LLM per query.
            </div>
          </div>
          <div class="diagram-node">
            <div class="diagram-node-title">graphify</div>
            <div class="diagram-node-body">
              Per-repo code graph — clusters, god nodes, call paths. Tree-sitter AST; optional LLM for docs.
            </div>
          </div>
          <div class="diagram-node">
            <div class="diagram-node-title">
              <span class="scadrial-only">Forge</span>
              <span class="classic-only">Group</span>
            </div>
            <div class="diagram-node-body">
              Cross-repo merge layer — name-match + HTTP-route edges tagged with INFERRED provenance.
            </div>
          </div>
        </div>
      </section>
    </main>

    <footer>
      <div class="footer-badges">
        <a href="https://github.com/Nikitzu/metalmind/blob/main/LICENSE">
          <img src="https://img.shields.io/badge/license-MIT-93b1d0?style=flat-square&labelColor=10141b" alt="MIT license" />
        </a>
        <a href="https://github.com/Nikitzu/metalmind">
          <img src="https://img.shields.io/github/stars/Nikitzu/metalmind?style=flat-square&color=d4a14a&labelColor=10141b" alt="GitHub stars" />
        </a>
      </div>
      <p>Inspired by Brandon Sanderson's Mistborn Era 1 novels. Not affiliated.</p>
      <p style="margin-top:0.25rem;opacity:0.6;">Built on <a href="https://obsidian.md">Obsidian</a>, <a href="https://ollama.com">Ollama</a>, <a href="https://qdrant.tech">Qdrant</a>, <a href="https://github.com/oraios/serena">Serena</a>, <a href="https://pypi.org/project/graphifyy/">graphify</a>.</p>
    </footer>

    <!-- asciinema-player lazy-loaded after first paint, only if the cast exists -->
    <script>
      const ASCIINEMA_VERSION = '3.8.1';
      const castUrl = '/wizard-demo.cast';

      const loadPlayer = () => {
        if (document.getElementById('asciinema-css')) return;
        const css = document.createElement('link');
        css.id = 'asciinema-css';
        css.rel = 'stylesheet';
        css.href = `https://cdn.jsdelivr.net/npm/asciinema-player@${ASCIINEMA_VERSION}/dist/bundle/asciinema-player.css`;
        document.head.appendChild(css);
        const js = document.createElement('script');
        js.src = `https://cdn.jsdelivr.net/npm/asciinema-player@${ASCIINEMA_VERSION}/dist/bundle/asciinema-player.min.js`;
        js.onload = () => {
          // @ts-ignore
          if (window.AsciinemaPlayer) {
            const root = document.getElementById('cast-root');
            const placeholder = document.getElementById('cast-placeholder');
            if (root) {
              root.style.display = 'block';
              if (placeholder) placeholder.style.display = 'none';
              // @ts-ignore
              window.AsciinemaPlayer.create(castUrl, root, {
                theme: 'solarized-dark',
                fit: 'both',
                terminalFontFamily: "'JetBrains Mono', ui-monospace, monospace",
                poster: 'npt:0:04',
              });
            }
          }
        };
        document.head.appendChild(js);
      };

      const maybeLoad = async () => {
        try {
          const res = await fetch(castUrl, { method: 'HEAD' });
          if (res.ok) loadPlayer();
        } catch {
          /* keep placeholder */
        }
      };

      if ('IntersectionObserver' in window) {
        const root = document.querySelector('.cast-wrap');
        if (root) {
          const io = new IntersectionObserver(
            (entries) => {
              for (const entry of entries) {
                if (entry.isIntersecting) {
                  maybeLoad();
                  io.disconnect();
                  break;
                }
              }
            },
            { rootMargin: '200px' },
          );
          io.observe(root);
        }
      } else {
        maybeLoad();
      }
    </script>
  </body>
</html>
