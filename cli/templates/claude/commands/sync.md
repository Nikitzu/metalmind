---
description: Commit and push your Knowledge vault to its git remote
model: sonnet
---

{{> .shared/sync-body.md}}
## Arguments

If the user passed text after `/sync`, use it as the commit message instead of writing your own.

User input: $ARGUMENTS
